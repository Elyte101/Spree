# Fix Log

| Issue | Root Cause | Fix | Test |
|-------|-----------|-----|------|
| C1 | Ledger service existed but was never called; `_mark_order_paid`, `confirm_delivery`, `auto_release_delivered_orders`, and `refund_order` all moved money without writing any ledger entries. | Added `from app.services import ledger as ledger_svc` import. In `_mark_order_paid`: write `PAYMENT_RECEIVED`, `PROCESSING_FEE_HELD`, and per-seller `COMMISSION_HELD`+`SELLER_CREDIT` in the same transaction. In `_release_payout`: write `PAYOUT_INITIATED` (buyer-confirm) or `AUTO_RELEASE` (cron) + `PAYOUT_FAILED` on error, all in same transaction. In `refund_order`: write `REFUND_INITIATED` before the Paystack call. | 48 pytest tests pass (all existing order/payment flow tests cover these code paths). |
| H6 | Payout logic (~90 lines: compute seller_payouts, increment counters, commit, initiate Paystack transfers) was hand-copied identically between `confirm_delivery` and `auto_release_delivered_orders`. | Extracted `_release_payout(db, order, *, is_auto_release=False) -> dict[str, tuple[Decimal, bool]]`. Both callers now call it after setting `order.status = "confirmed"`. Returns `{seller_id: (payout_ghs, transfer_ok)}` so callers format their own seller notifications. | 48 pytest tests pass. |
| H7 | In `auto_release_delivered_orders` the `except` block logged `order.id` but `order` could be unbound if the `db.scalar()` call itself raised. | Changed `logger.error("auto_release failed for order %s: %s", order.id, exc)` → `order_id` (the loop variable string, always bound). | Covered by the auto_release code path. |
| C2 | `cancel_order` allowed buyers to cancel paid orders. Used `db.get()` (no lock) so two concurrent cancels could race. | Added buyer guard: non-admin callers get 409 if `order.status == "paid"`. Changed `db.get()` → `db.scalar(select(Order).with_for_update())`. | 48 pytest tests pass. |
| C3 | Failed Paystack transfers left orders permanently stuck in "confirmed" with no retry path. `transfer.success`/`transfer.failed` webhooks only logged; payout state was untracked. | (a) `transfer.success` webhook now writes `PAYOUT_CONFIRMED` and calls `_maybe_advance_to_paid_out` to set order to `paid_out` when all sellers confirmed. (b) `transfer.failed` webhook now writes `PAYOUT_FAILED` to ledger. (c) Added `retry_stuck_payouts(db)` that finds "confirmed" orders with missing `PAYOUT_CONFIRMED` entries and re-attempts transfers with the same idempotency key. (d) Added `POST /cron/retry-payouts` endpoint. | 48 pytest tests pass. |
| C4 | Verification sessions and rate-limit buckets were in-process dicts; Vercel serverless means `/identity/face-verify` would always 404 "session not found" in production. | Added `IdentitySession` and `RateLimitEvent` DB models. Rewrote `verification_session.py` to use DB (same API, `db: Session` added as first arg). Rewrote `_check_rate_limit` in `deps.py` to query/insert `RateLimitEvent` rows. Updated all callers in `identity.py` to pass `db`. | 48 pytest tests pass. |
| C6 | `stream_webhook` skipped signature verification when `STREAM_WEBHOOK_SECRET` was empty. Anyone could POST fake events → burn AI credits and post spoofed replies. | Added `_check_stream_config()` startup log. In the handler: if deployed and secret is unset, return 503 immediately (fail closed). When set, verification is always performed (unchanged). | 48 pytest tests pass. |
| H1 | `_server_totals` accepted items without a `productId`, so guest/custom items with no DB record could enter orders — bypassing stock checks and leaving `seller_id`/`commission_rate` as NULL. | Added early 422 check: if `item.productId` is falsy, raise immediately. | Covered by existing server_totals code path; 48 tests pass. |
| H2 | `charge.success` webhook for already-cancelled orders was silently dropped; buyer was charged with no refund or ledger record. | Added branch: if `order.status == "cancelled"`, write `PAYMENT_RECEIVED` ledger entry then immediately call Paystack refund + write `REFUND_INITIATED` + set `order.status = "refunded"`. | 48 tests pass. |
| H3 | Idempotent replay of `initialize_payment` returned an empty `authorizationUrl`, stranding the buyer on a blank page. | Stored `paystack_access_code` on the `Order` model (new column via `_COLUMN_MIGRATIONS`). On replay, reconstruct URL as `https://checkout.paystack.com/{access_code}`. | 48 tests pass. |
| H4 | Backend returned the first DB cart with no user scope — any caller got someone else's cart. Frontend also fetched it on every layout render (cold-start waterfall). | Deleted `backend/app/api/routes/cart.py`, removed `cart` router include, deleted `app/api/cart/route.ts`, removed `getCart` from `serverApi.ts` and `lib/api/index.ts`, simplified `CartProvider` to a no-op wrapper. Cart lives entirely in Zustand localStorage. | 48 tests pass; `npm run build` clean. |
| H5 | `pricing.py` globals were set once at startup and never refreshed; serverless invocations always saw cold-start defaults, ignoring admin DB overrides. | Added `refresh_if_stale(db)` with 60 s TTL. Called at the top of `_server_totals` so every order initialization re-reads current pricing from DB. | 48 tests pass. |
| H6 | (see above — extracted as prerequisite for C1/C3) | | |
| H7 | (see above) | | |
| H8 | `create_order` (admin endpoint) trusted client-supplied `subtotal`, `total`, and per-item `price`; never decremented stock; never wrote ledger entries. | Replaced hand-rolled order construction with the same safe path as `initialize_payment`: call `_server_totals` (ignoring client prices), `_check_stock`, `_build_pending_order`, then `_mark_order_paid` (which decrements stock and writes all ledger entries). | Added `test_admin_create_order_recomputes_totals_and_writes_ledger`; 49 tests pass. |
| M1 | Stream singleton `_client` in `ChatWidget` persisted across user sessions — logging out didn't disconnect the old user, so the next user would connect over a stale socket. | Added `useEffect` tracking `session.user.id` via `prevUserIdRef`. On id change (logout or user switch), calls `_client.disconnectUser()`, nulls the singleton, and resets all local state. | 49 tests pass; TypeScript clean. |
| M2 | `upsert_user` in `chat_token` set `"name": actor_id` — Stream showed raw user IDs instead of real names in the admin chat view. | Added `db.get(User, actor_id)` to look up the user's real name; passes `display_name = user.name or actor_id` to `upsert_user`. | 49 tests pass. |
| M3 | Stream webhook called Claude synchronously — if Claude took >3 s Stream would time out the webhook and retry, causing duplicate AI replies. | Extracted AI reply logic into `_post_ai_reply` background function. Added in-process `_processed_message_ids` set for dedup (best-effort on warm invocations). Webhook returns 200 immediately; AI task runs after response. | 49 tests pass; existing webhook tests pass. |
| M4 | `/uploads/{path}` route served identity document files from ephemeral `/tmp` — useless in Vercel serverless and leaked a file-serving attack surface. `uploads.py` service was dead code. Admin vendor documents route fetched non-existent upload columns. | Deleted `serve_upload` route from `main.py`, deleted `backend/app/services/uploads.py`, deleted `app/api/admin/vendors/[id]/documents/route.ts`. | 49 tests pass. |
| M5 | `lookup_ghana_card` only saved `government_id_number+hash` if the field was empty — re-verification with a different card left stale data. Face-verify success also didn't re-persist the winning card. | Removed `if not user.government_id_number:` guard in lookup. Face-verify success block now always writes `government_id_number = encrypt(session.id_number)` and `government_id_hash`. | 49 tests pass. |
| M6 | Catalog service returned money fields as JSON floats (`float(listed_price)`) — susceptible to float serialization imprecision. | Renamed `_quantize_money` → `_money_str` returning `str`. Changed `price`, `sellerPrice`, `originalPrice` to string in catalog response. Updated `Product.price` TS type to `string \| number`, `formatPrice` to accept `number \| string`, and fixed 4 math sites (`productCard`, `productListingPage`, `checkoutPage`, `cartStore`). | 49 tests pass; tsc clean. |
| M7 | 5 `react/no-unescaped-entities` ESLint errors in `Step4Identity.tsx` (apostrophes in JSX). | Replaced `'` → `&apos;` at lines 165, 266, 284, 350, 390. | ESLint: 0 errors. |
| M8 | `requirements.txt` missing trailing newline. | Added newline. | — |
| M9 | SPEC_GAPS.md had G24 as "missing" even though Stream Chat now implements buyer/seller→admin chat. | Updated G24 to "done" with implementation details. | — |
| M10 | `processing` and `pre_transit` order states were defined in the type system and frontend maps but never set by any backend function. | Removed both states from `OrderStatus` type, all frontend status maps, the refund guard, `canCancel`/`canRefund` logic, and `orderTrackingPage` normalizer. Logistics adapter's tracking event type `pre_transit` (different from order status) remains. | 49 tests pass; tsc clean; build: 70/70 pages. |
| CH1 | Unread badge always showed 0 — `useChatUnread` used private `StreamChat._instances` API with a 1 s timeout and only ran once; if chat wasn't opened yet the client didn't exist. | Created `components/providers/chatProvider.tsx` — `ChatProvider` wraps the layout and connects eagerly on login. `useChatConnection()` context hook exposes `{client, channel, connectStatus, unreadCount, markRead, retry}`. `useChatUnread` reduced to a one-liner reading `unreadCount` from context. | 56 tests pass; tsc clean. |
| CH2 | `/chat` page redirected to `/login?next=/chat` — a route that doesn't exist, sending users to a 404. | Changed to `redirect("/auth/sign-in?callbackUrl=/chat")`. | tsc clean. |
| CH3 | AI reply was posted via `BackgroundTasks` — on Vercel serverless, the process is killed after the response is sent, so the background task never runs. | Removed `BackgroundTasks`; `_post_ai_reply` is now called inline before `return {"ok": True}`. Stream's 30 s webhook timeout gives enough headroom for Claude Haiku (typically 2–4 s). | 56 tests pass. |
| CH4 | AI bot replied to all channel types — DMs, team channels, etc. | Added early return if `channel_type != "support"` or `!channel_id.startswith("support-")`. | 56 tests pass. |
| CH5 | No notification bridge: user never knew the bot replied; admin only got a dev-notifier email for the first message. | `_post_ai_reply` calls `notify_safe` (in-app + email) to the buyer after posting a reply. Dev-notifier now fires for every user message (not just first). Added `"chat_reply"` event type to `DEFAULT_PREFS` in `notifications.py`. | 56 tests pass. |
| CH6 | Stream event listeners were added on every reconnect in `ChatWidget` with no cleanup — each retry leaked 4 listeners. | Listeners are registered once in `ChatProvider`'s effect; the effect cleanup removes them precisely via a captured `removeListeners` closure. | tsc clean. |
| CH7 | No AI rate limiting — a user could spam messages and drain Anthropic credits. | Added DB-backed rate limiting via `_check_rate_limit`: 1 AI reply per channel per 20 s, 20 per channel per hour. Rate-limited webhook returns `{"ok": True}` (not 429) so Stream doesn't retry. | 56 tests pass. |
| CH8a | Admin chat channel list hidden on mobile (`display: none` below `md`) — mobile admins saw an empty pane. | Added `mobileView` state + `MobileViewWatcher` (reads Stream context to detect selection). CSS switches between list and channel pane on mobile; a back button returns to the list. | tsc clean. |
| CH8b | Admin chat error state had no retry button. | Added "Retry" button to the error state that increments `retryCount` to trigger a fresh connection. | tsc clean. |
| CH8c | Admin token fetch had no timeout. | Added `AbortController` with 15 s timeout to the admin-token `fetch`. | tsc clean. |
| CH8d | Per-admin identity in Stream: should each admin connect as their real user ID or as `spree-admin`? | Kept shared `spree-admin` identity (user decision — simpler, no migration needed). | — |
| CH8e | `_adminClient` singleton was never disconnected when the admin chat page unmounted — leaving a stale WebSocket connection. | Added cleanup in `useEffect`: `_adminClient` is nulled immediately and disconnected async. | tsc clean. |
| CH8f | `catch (err)` had an unused variable causing a lint warning. | Changed to `catch` (no binding). | tsc clean. |
| CH8g | `ChannelList` filter `{ type: "support" }` showed all support channels regardless of membership — would expose other users' channels if misconfigured. | Filter is now `{ type: "support", members: { $in: [adminId] } }`. `adminId` comes from the token response. | tsc clean. |
| CH9 | Chat FAB appeared for admin users; AI would reply to any admin-opened support channel. | `ChatWidget` returns `null` when `session.user.role === "admin"`. AI already skips `spree-admin` sender messages (unchanged). | tsc clean. |
| CH10 | Chat FAB overlapped the full-page `/chat` and `/dashboard/chat` routes — two chat UIs visible at once. | `ChatWidget` uses `usePathname()` and returns `null` on those routes. | tsc clean. |
| CH11 | First-message check ran a synchronous Stream `channel.query()` inline in the webhook handler, adding latency to every webhook response. | Removed the first-message gate entirely; dev-notifier now fires for all messages (CH5). The Stream query is still done inside `_post_ai_reply` for the admin-recent-reply check (already there). | 56 tests pass. |
| CH12 | Dead `import json as _json` in `_post_ai_reply`; dedup set cleared with `_processed_message_ids.clear()` at 1000 — causing a momentary window where duplicates could slip through. | Removed dead import. Changed `set` to `deque(maxlen=1000)` — oldest entry is evicted automatically, no clear needed. Changed `.add()` → `.append()`. | 56 tests pass. |
| CH13 | AI bot had no order context — couldn't answer questions about the user's specific orders. | `_post_ai_reply` queries the last 5 orders for the channel's user and appends them (id, status, total, shipped date, ETA) to the system prompt. | 56 tests pass. |
| CH14 | No documentation for Stream Chat setup. | Added "Chat setup" section to `README.md` covering channel type, webhook URL, env vars, and flow description. Both `.env.example` files already contained Stream variables (no change needed). | — |
| P1 | Payout method "card" removed per owner decision; bank transfer reinstated. "card" was never wired to an actual Paystack recipient type — it produced no transfer. Method changed to `"mobile_money" \| "bank"`. `cardLast4`/`cardholderName` fields removed; `bankCode`/`bankName`/`accountNumber` added to all payout schemas (backend + TS). `Step5Payout.tsx` and `profilePage.tsx` rewritten. Bank list served from Paystack via `GET /auth/banks`; account name resolved via `GET /auth/banks/resolve` (`useBankResolve` hook). | 64 pytest pass; tsc clean. |
| P2 | Step 5 never created a Paystack transfer recipient — sellers could complete onboarding without a working payout account, then fail silently at payout time. | `save_step5` now calls `paystack_svc.create_transfer_recipient` at save time for both MoMo and bank methods. Returns 502 if creation fails so the seller can fix their details before submitting. `submit_onboarding` validates `payout_info` exists and (when Paystack is configured) `paystack_recipient_code` is set. | 64 pytest pass. |
| P3 | Both seller-activation paths (face-verify auto-approve and admin `approve_seller`) silently activated sellers without checking for a payout recipient — future payouts would silently fail. | Face-verify auto-approve now checks `paystack_recipient_code` before setting `seller_status = "active"`; if missing, returns `verified=True` but keeps current status and notifies seller to add payout details. `approve_seller` raises 400 if Paystack is configured and `paystack_recipient_code` is missing. | 64 pytest pass. |
| P4 | `_release_payout` for sellers without a recipient code wrote `payout_status = "pending_account"` but no ledger entry and no dev alert — the missing payout was invisible to monitoring. | Added `ledger_svc.record_payout_failed(…, reason="missing_recipient")`, an honest seller notification ("add your payout details to receive GHS X.XX"), and `dev_notifier.alert("payout_missing_recipient", …)`. | 64 pytest pass. |
| P5 | `retry_stuck_payouts` only queried `payout_status IN ("processing", "failed", "reversed")` — excluded "pending_account" orders, so sellers who later added payout details never got retried. | Added `"pending_account"` to the status filter. Also added it to the payout_status advance condition so it transitions to `"processing"` on retry. | 64 pytest pass. |
| P6 | `update_payout_info` only created Paystack recipients for MoMo, not bank. After saving new payout details, there was no retry of stuck payouts — a seller adding their details would wait for the hourly cron. | Added bank recipient creation to `update_payout_info`. Added `retry_stuck_payouts(db)` call immediately after saving a valid recipient code. Added `app/api/cron/retry-payouts/route.ts` (mirrors auto-release cron) and hourly `vercel.json` cron entry. | 64 pytest pass. |
| P7 | `_MOMO_NETWORK_BANK_CODE` in `orders.py` only mapped short codes ("MTN"/"VOD"/"ATL") but `ghana.ts MOMO_NETWORKS` stores full names ("MTN Mobile Money"/"Telecel Cash") — `_ensure_recipient_code` always got `bank_code=""` and returned `None`. | Extended `_MOMO_NETWORK_BANK_CODE` to also accept lowercase full network names. Case-insensitive fallback lookup added in both `_ensure_recipient_code` and `update_payout_info`. | 64 pytest pass. |
| A1 | `auth.config.ts` signed JWTs with a hardcoded fallback secret (`"spree-dev-secret-change-me"`) whenever `NEXTAUTH_SECRET`/`AUTH_SECRET` was unset — fails open, letting anyone forge an admin session. `BACKEND_INTERNAL_API_KEY`'s only guard was a dead warning (checked against a value the field's own default never produces). | `auth.config.ts` now calls `getNextAuthSecret()`, which throws at import time in deployed environments if unset. Added `requireStrongSecret()` in `runtimeConfig.ts`: throws if a deployed env's `NEXTAUTH_SECRET`/`BACKEND_INTERNAL_API_KEY` is unset, equals the dev default, or is <20 chars. Mirrored server-side in `backend/app/core/config.py`'s `require_strong_secret_when_deployed` validator (raises `ValueError` → app fails to boot). Also set `session.maxAge` to 24h (A10). | 64 pytest pass; tsc clean. |
| A2 | FastAPI derived actor identity entirely from client-suppliable `X-Actor-User-Id`/`X-Actor-Role` headers, gated only by the shared internal key — anyone who reached the backend directly with a leaked/guessed key (backend is publicly deployed) got instant admin. Two routes (`products_create`, `order_refund`) read the raw headers directly, bypassing even the shared dependency layer. | Added `lib/actorToken.ts`: mints a 60s HS256 JWT (`jose`, sub=userId, role, iss/aud) from a new `ACTOR_TOKEN_SECRET`. `fetchBackend()` in `serverApi.ts` mints this token automatically whenever a caller sets `X-Actor-User-Id` on an internal call — the ~40 existing route files needed no changes. `deps.py`'s `_get_actor_user_id`/`_get_actor_role` now decode+verify `X-Actor-Token` (PyJWT); a present-but-invalid/expired token is rejected with 401 rather than falling back to anonymous. Plain `X-Actor-*` headers are no longer trusted for authorization (kept only for log correlation). Fixed 4 call sites in `serverApi.ts` (`getAdminOverview`, `getAdminSellers`, `getAdminTopProducts`, `getProducts`) and 3 route files that set `X-Actor-Role: admin` with no user id at all. Switched `products_create` and `order_refund` off their raw `Header(alias="X-Actor-...")` reads onto the verified `ActorUserId`/`ActorRole` deps. `ACTOR_TOKEN_SECRET` added to both `.env`/`.env.example` pairs, guarded by the same `requireStrongSecret`/`require_strong_secret_when_deployed` fail-closed checks as A1. | Added `conftest.actor_token()` test helper + 3 new tests (forged-secret token, expired token, plain-header-no-longer-grants-admin all → 401/403). 67 pytest pass; tsc clean; vitest 68 pass. |
| A3 | `upsert_oauth_user` auto-linked any OAuth sign-in to an existing account by email match — no check that the provider actually verified the email, no check that the account even had a password to protect. An attacker registering a Google account with a victim's email could sign into the victim's password-protected Spree account. | `upsert_oauth_user` now only auto-links a first-time OAuth sign-in to an *existing* account when the provider's `email_verified` claim is `true` (403 otherwise) **and** the account has no password set (409 otherwise) — repeat logins to an already-linked account are unaffected. New-user creation now records the provider's real `email_verified` claim instead of hardcoding `True`. `auth.ts`'s `jwt` callback reads `profile.email_verified` (handles Google's boolean and Apple's sometimes-string `"true"`) and forwards it to `/auth/oauth-upsert`; the existing "deny sign-in on non-2xx" path in `jwt()` already turns the new 403/409 into a clean sign-in denial. | 3 new tests: unverified-claim link denied (403, victim's password login still works), verified-but-existing-password link denied (409), fresh email creates a new user. 70 pytest pass; tsc clean; vitest 68 pass. |
| A4 | `email_verified` was captured at signup/OAuth but nothing ever checked it — unverified accounts could check out, submit a seller application, and post product comments/reviews, making the whole verification flow cosmetic. Policy decision (owner): gate sensitive actions, not login/browsing. | Added `require_email_verified(db, user_id, action)` in `services/auth.py` — no-op for guests (`user_id is None`), 403 for a signed-in user whose `User.email_verified` is `False`. Wired into `initialize_payment` and `charge_momo_payment` (checkout — guest checkout unaffected since `payload.userId` is `None`), `submit_onboarding` (seller application submission), and `create_comment` (product comments/reviews). Login itself and all read/browse routes are untouched. | 5 new tests: unverified signed-in buyer blocked from checkout (403) while guest checkout still succeeds, unverified seller blocked from onboarding submit (403), unverified user blocked from posting a comment (403). 74 pytest pass. |
| A5/A7 | Login lockout (`lib/rateLimit.ts`) was a module-level `Map` — resets on every Vercel cold start, and invisible to anyone calling the FastAPI backend's `/auth/login` directly instead of through Next. `auth.ts`'s `authorize()` also had its own separate, buggier copy: a hardcoded `ipKey = "login-ip:server"` meant one attacker's failures locked out every user's IP bucket collectively, and per-IP limiting never actually happened. A second, better wrapper (`app/api/auth/[...nextauth]/route.ts`) already existed with correct real-IP extraction, but it's still in-memory and only covers requests that go through Next. | Added `check_login_rate_limit`/`record_login_failure`/`clear_login_failures` to `services/auth.py`, DB-backed via `RateLimitEvent` (same table as C4), keyed on **both** `login:email:{email}` and `login:ip:{ip}` — 5 attempts/15 min, either key alone locks out the attempt. Wired into `POST /auth/login` in `routes/auth.py`, which now takes `X-Client-Ip` (set by the Next proxy from the real browser IP) with a fallback to the backend's own `X-Forwarded-For` (set by Vercel's edge) for callers that bypass Next entirely. `auth.ts`'s `authorize()` no longer does its own buggy in-memory bookkeeping — it forwards the real client IP (from the `request` param NextAuth's Credentials provider exposes) and denies sign-in with a `rate_limited` `CredentialsSignin` code on a 429 from the backend. The existing Next-side wrapper in `[...nextauth]/route.ts` is left as a harmless best-effort fast-path (unchanged, its own tests still pass); the backend is now the authoritative, un-bypassable enforcement point. | 5 new backend tests: per-email lockout, per-IP lockout across different emails, lockout still enforced when calling the endpoint directly with no `X-Client-Ip` (only `X-Forwarded-For`, simulating a proxy-bypassing attacker), and successful login clearing prior failures. 78 pytest pass; tsc clean; vitest 68 pass. |
| L1 | `npm run lint` (`eslint .`) crashed repo-wide with "could not find plugin react" — unrelated to this audit, but discovered while running the required `npm run lint && npm run typecheck && npm run test` after each fix. Root cause: `eslint.config.mjs`'s `globalIgnores()` didn't include `.vercel/**` (gitignored local Vercel CLI build-output cache); once a `vercel build`/`vercel dev` had run locally, `eslint .` walked into the compiled/minified bundles under `.vercel/output/functions/**/*.func` and choked on them. | Added `.vercel/**` to `eslint.config.mjs`'s `globalIgnores()` list, alongside the existing `.next/**`/`out/**`/`build/**` entries. | `npm run lint` now runs clean repo-wide: 0 errors, 18 pre-existing warnings (none introduced by this pass). Confirmed by running the full `npm run verify` end-to-end (see summary below). |
| A11 | `_validate_password_strength` required upper/lower/digit/symbol composition with no max length (scrypt DoS via a huge input) and no check against common/breached passwords — the mandatory composition rule also pushes users toward predictable substitutions (`P@ssw0rd1`) without materially raising entropy. Owner decision: NIST 800-63B-style — length-first, drop composition, add a common-password blocklist. | Rewrote `_validate_password_strength`: 8-char minimum (unchanged), added a 128-char maximum, dropped the upper/lower/digit/symbol requirement entirely, and added a case-insensitive check against a new `app/core/common_passwords.py` blocklist (~150 curated common/breached passwords — keyboard patterns, "password"/"letmein"/"admin" variants, top numeric sequences; documented as a starting point, not a full breach corpus — a live Have I Been Pwned Pwned-Passwords check would be the natural next step if this needs to get stronger). `SignupRequest`/`PasswordResetConfirmIn` schemas already capped at 128 chars (A6), so the service-level max-length check is defense-in-depth. Updated the sign-up form's helper copy to match (dropped the "uppercase, lowercase, number, symbol" line). | 5 new tests: common password rejected, case-insensitive match, a long all-lowercase passphrase with no digits/symbols now succeeds (proves the composition rule is gone), a 200-char password rejected, and password-reset confirm applies the same blocklist check. 91 pytest pass; tsc clean; eslint clean on changed file. |
| A10 | Session `maxAge` was unset (30-day default — fixed in A1 to 24h), and every `actor_role`/`actor_id` check across ~20 call sites trusted the role claim baked into the signed actor token, which reflects the Next-side session at mint time. A seller blacklisted, soft-deleted, or demoted by an admin kept acting under their old role for up to a full session lifetime. | `deps.py`'s `_get_actor` (the single function every `ActorUserId`/`ActorRole` dependency resolves through) now takes `db: DBSession` and re-fetches the `User` row by the token's `sub` claim on every request, discarding the token's `role` claim entirely in favor of the current DB value; a missing/blacklisted/soft-deleted user is downgraded to fully anonymous (`None`, `"customer"`) rather than trusting stale claims. This benefits every one of the ~20 existing `actor_role != "admin"` checks with zero changes to those call sites — the same "one chokepoint" pattern as A2's actor-token minting. **Scope note:** this does not yet invalidate a *specific* JWT session immediately after a password reset (A6) — that would require threading the original session's issued-at through the actor token to every one of the ~40 `X-Actor-*`-setting call sites (the same class of change A2 deliberately avoided), which is deferred; `User.password_changed_at` is tracked and available for that follow-up. | 3 new tests: a blacklisted admin's still-valid-looking token loses access immediately, a soft-deleted user is treated as anonymous, and a demoted vendor's stale "vendor" claim is overridden by the current (lower) DB role. Also fixed one pre-existing test (`test_chat_token_returns_503...`) that used a synthetic non-existent user id — now correctly downgraded to anonymous, so it needed a real signed-up user like every other actor-scoped test already used. 86 pytest pass. |
| A9 | Needed to confirm `isSafeCallbackUrl` actually rejects `//evil.com`, `https://evil.com`, `/\evil.com`, `javascript:`, and that the OAuth `jwt` callback's `return null as unknown as typeof token` on backend-sync failure denies sign-in cleanly rather than producing a half-populated token (no id/role, but signed in). | `lib/safeUrl.test.ts` already existed and already covered every case named in the audit plus several more (subdomain/userinfo tricks) — no gap there, verified by inspection. For the OAuth-sync-failure path: extracted `callBackend`/`syncOAuthUser`/`AppUserRole` out of `auth.ts` into a new `lib/authBackend.ts` (importing `next-auth`/providers under vitest's plain Node environment fails to resolve `next/server` internals, so the pure backend-sync logic needs to live somewhere importable without them). `auth.ts`'s `jwt` callback now calls `syncOAuthUser` and denies (`return null as unknown as typeof token`) only when it returns `null`. Added a `server-only` → no-op stub + vitest alias (`lib/__stubs__/server-only.ts`) since that marker package isn't installed as a real dependency (Next's bundler special-cases it; plain Node/vitest can't resolve it). | New `lib/authBackend.test.ts`: 5 cases proving `syncOAuthUser` only ever returns fully-populated (`id`+`role`+`emailVerified`) or `null` — success, non-2xx (403/409 from A3), network failure, timeout, and an explicit "no partial object" shape check. 73 vitest pass (up from 68); tsc clean; 83 pytest pass (unaffected). |
| A8 | `emailVerified` was baked into the JWT at login and never refreshed; the `jwt` callback's `trigger === "update"` branch existed (`{ ...token, ...session }`) but nothing ever called `useSession().update()`, and it blindly trusted whatever the caller passed in — a client could have set `emailVerified`/`role` itself. A user who verified their email after logging in stayed unverified in their session until a full re-login. | `jwt` callback's update branch now ignores the client-supplied `session` payload and re-fetches the canonical `email_verified`/`role` from `GET /auth/profile/{id}` (minting a signed actor token per A2, since the backend no longer trusts a plain header) — a transient fetch failure leaves the token unchanged rather than invalidating the session. Wired `useSession().update()` into `app/auth/verify-email/page.tsx` after a successful verification, so the session refreshes without a re-login. | tsc clean; eslint clean on changed files; vitest 68 pass; 83 pytest pass (unaffected). |
| A6 | No password-reset flow existed at all — a locked-out user had no self-service recovery path, and support had no mechanism either. | Added `purpose` column to `VerificationToken` (default `"email_verification"`) so the existing email-verification tokens and new password-reset tokens share one table without being replayable against each other; added `password_changed_at` to `User` for session invalidation (A10 wires the actual check). `services/auth.py`: `request_password_reset` (silent no-op for unknown emails — no enumeration signal; 1h-expiry token emailed via `notify_safe`/Resend, added `password_reset` to `MANDATORY_EVENTS` so it can't be muted via notification prefs) and `reset_password_with_token` (validates strength, hashes, bumps `password_changed_at`, deletes the token and any other outstanding reset tokens for that email — one-time use). New routes `POST /auth/password-reset/request` (rate-limited 3/hour/email via the existing `_check_rate_limit`) and `POST /auth/password-reset/confirm`. Next side: `PasswordResetRequestSchema`/`PasswordResetConfirmSchema` in `lib/validation/auth.ts`, thin proxy routes under `app/api/auth/password-reset/`, `api.requestPasswordReset`/`api.confirmPasswordReset` client methods, new `/auth/forgot-password` and `/auth/reset-password` pages (mirroring the existing `/auth/verify-email` page style), and a "Forgot password?" link on the sign-in form. | 5 new backend tests: happy path (old password stops working, new one works), single-use token (second confirm attempt 400s), expired token rejected, request-endpoint response is identical for a known vs. unknown email, and a password-reset token can't be replayed against `/auth/verify-email` (purpose scoping). 83 pytest pass; tsc clean; eslint clean on new/changed files; vitest 68 pass. |
| EMAIL-0 | 2026-07-10 email flow assessment, STEP 0: two Resend sender domains in use — backend `email_from` defaulted to `"Spree <no-reply@spree.com>"`, frontend `EMAIL_FROM` defaulted to `"Spree <noreply@spree.market>"`. Neither is a domain the project owns/controls DNS for, so neither could ever be verified in Resend — one (or both) sends silently failed on every call (`_send_email`/`sendVerificationEmail` both swallow errors with no detail). Owner has no verified custom domain yet (checked: `spreecommerce.vercel.app` can't be verified either — Resend requires DNS-level control, and Vercel subdomains aren't controllable that way). | Owner decision: both now default to Resend's sandbox sender `"Spree <onboarding@resend.dev>"` (works with zero domain verification; only delivers to the Resend account's own email — real customer delivery requires verifying a real domain later and setting `EMAIL_FROM` to the same value on both sides). Updated `backend/app/core/config.py` and `lib/email.ts` defaults, plus both `.env.example` files with explicit "must match" callouts. Added structured logging to both send paths: backend `_send_email` (`app/services/notifications.py`) now logs `email_status`/`resend_message`/`email_from` via the existing `safe_extra()` helper (never uses the reserved `message` key) on success, and `email_status="failed"`+full error on failure; also logs `email_status="skipped_no_api_key"` when `RESEND_API_KEY` is unset (previously a silent `logger.debug`). Frontend `sendVerificationEmail` (`lib/email.ts`) logs `email_status`/`resend_message` on success and `email_status="failed"`+error message on failure (still re-throws, preserving the existing caller contract). **Not yet done:** setting `EMAIL_FROM`/matching `RESEND_API_KEY` on the actual Vercel projects — proposed as a follow-up deploy step, pending explicit confirmation (same pattern as the `ACTOR_TOKEN_SECRET` incident). | Covered by EMAIL-2's `test_email_flow.py` (structured-logging assertions on the success/failure/skip paths). |
| EMAIL-1 | 2026-07-10 email flow assessment, STEP 1: `register_user()` created accounts with `email_verified=False` but never sent a verification email, and the signup flow never called the existing verification pipeline (`create_verification_token` + `/auth/verify-email`, both pre-existing and reused as-is — not rebuilt). | Trigger point: the **frontend** signup route (`app/api/auth/signup/route.ts`), not the backend, because the user has no session yet at signup time — the only existing consumer of the pipeline (`app/api/auth/send-verification/route.ts`) requires `auth()`, which doesn't apply here. This is not "moving the send to the backend" (no ask needed per the task's own criteria): it reuses the exact existing two-hop pipeline (backend mints a token via `/auth/send-verification`, frontend's existing `sendVerificationEmail` sends it) from a new call site. Added `sendSignupVerificationEmail()` helper in the signup route, called after a successful (`response.ok`) signup; wrapped in try/catch so a failure is logged and swallowed — the 201 the backend already returned is untouched either way. Awaited (not fire-and-forget) because Vercel serverless functions stop executing once the response is sent (confirmed non-viable pattern — see FIXLOG `CH3`), so a detached promise would frequently never complete; timeouts are kept tight (3s token fetch + 6s Resend send, see EMAIL-2) to bound the added latency. | 4 new vitest tests (`signup-verification-email.test.ts`): exactly one verification email sent on success with the right email+token; 201 preserved when token generation fails; 201 preserved when the Resend send itself throws; no email attempted when signup fails Next-side zod validation. Backend: `test_send_verification_then_verify_email_flips_flag` proves the reused pipeline still works end to end (signup → send-verification → verify-email → `email_verified` flips true, reflected on next login). |
| EMAIL-2 | 2026-07-10 email flow assessment, STEP 2: `/auth/password-reset/request` intermittently timed out at the Next proxy's 15s `AbortSignal.timeout` (`upstream_unreachable`). Root cause confirmed by reading the installed `resend` package source: `resend.http_client_requests.RequestsClient` defaults to a **30-second** HTTP timeout, applied process-wide via `resend.default_http_client` — every `_send_email()` call (including the one `notify_safe(event_type="password_reset")` already makes) blocked on that same unbounded client. Under any Resend slowness, the backend could legitimately take up to 30s to respond — well past the Next proxy's 15s budget — while (a) the backend route itself is otherwise reachable and fast (empirically ~1s for a real user, confirmed live against the deployed backend) and (b) local live testing didn't force-reproduce a full 30s hang, but the 30s-vs-15s mismatch is a structural risk regardless of whether any single test run hits it. | Backend: `_configure_resend_client()` in `app/services/notifications.py` overrides `resend.default_http_client` with `RequestsClient(timeout=6)` (lazily, once) before every send — 6s leaves headroom under both the 15s proxy timeout and a possible 10s Vercel Hobby-plan function limit for the rest of the request's work (DB queries, etc). Frontend: `sendVerificationEmail` (`lib/email.ts`) gets the equivalent fix — the Resend **JS** SDK's typed `send()` options don't expose a `signal`/timeout (only `headers`/`idempotencyKey`), so it's wrapped in a manual `Promise.race`-style `withTimeout()` helper bounded to 6s instead. No change needed to the request/rate-limit/generic-response logic — it was already correct. | `test_password_reset_request_uses_bounded_resend_timeout` in `test_email_flow.py` goes through the **real** call chain (`POST /auth/password-reset/request` → `request_password_reset` → `notify_safe` → `_send_email` → `resend.Emails.send` → `RequestsClient.request` → `requests.request`) with only the innermost `requests.request` mocked, and asserts the captured call received `timeout=6` — proving the fix is actually threaded through, not just configured and unused. Plus `test_send_email_configures_bounded_resend_timeout`. |
| EMAIL-3 | 2026-07-10 email flow assessment, STEP 3: task's stated premise was "`_mark_order_paid()` is called from `verify_payment`, `submit_otp_for_order`, and `handle_paystack_webhook` with no guard, so webhook retries can double-send" order-confirmation emails. **Finding: premise does not hold.** All three call sites already guard before calling `_mark_order_paid`: `verify_payment` returns early if `order.status == "paid"`; `submit_otp_for_order`'s mock-mode branch requires `status == "pending"`; `handle_paystack_webhook`'s `charge.success` branch requires `order.status == "pending"` (with `.with_for_update(skip_locked=True)` row-locking, which is also safe for concurrent deliveries — reasoned through both lock-acquisition orderings). Verified empirically, not just by inspection: a new test calls `handle_paystack_webhook` twice sequentially for the same `charge.success` event/reference and asserts `order_placed` fires exactly once — it does, with **zero code changes**. Same result for `add_tracking`/`order_shipped` (second call raises 409 since status is no longer `"paid"`). | **No idempotency-stamp columns added** — per the task's own instruction ("if [the cause is] different than assumed, report that instead of adding idempotency blindly"), and since the specific mechanism described doesn't reproduce, adding `confirmationEmailSentAt`/`shippedEmailSentAt` would be unused complexity for a non-reproducing scenario. If order emails aren't actually arriving in practice, EMAIL-0 (the sender-domain fix, now with visible logging) is the far more likely explanation — this was the assessment doc's own top suspect. | Two new permanent regression tests locking in the current (correct) behavior: `test_webhook_retry_does_not_double_notify_order_placed`, `test_add_tracking_retry_does_not_double_notify_order_shipped` — both act as tripwires if a future refactor removes one of the existing status guards. |
| A12 | 2026-07-10 "half-done features" review, item #6: a password reset bumped `password_changed_at` (A6) and A10 made every actor-role check re-read the DB, but the *signed actor JWT itself* was never invalidated — a session established before a password reset kept working under its old token for up to the full 24h `maxAge` (A1), since nothing compared the token's mint time against the reset time. | Added `sessionIssuedAt` (Unix seconds), stamped once in `auth.ts`'s `jwt` callback only inside `if (user && account)` (a genuine sign-in) — never touched again, including on the `trigger === "update"` refresh (A8). Threaded through `mintActorToken` (`lib/actorToken.ts`) as a new `siat` claim, following A2's chokepoint pattern: `fetchBackend` (`lib/serverApi.ts`) now calls `auth()` internally to read the current session's `sessionIssuedAt` and passes it into the token it mints, so none of the ~40 existing `X-Actor-User-Id`-setting call sites needed changes. Backend `_get_actor` (`deps.py`) rejects (downgrades to anonymous) when `siat < password_changed_at`. A **missing** `siat` claim (e.g. a session minted before this deploy) is treated as "not applicable," not a failure — deliberately avoids mass-logging-out every already-active session the moment this ships. Also fixed the `account.provider !== "credentials"` OAuth-routing check to `account.type !== "credentials"` while touching this code, since the new passkey provider (PK1) is a second Credentials-type provider with a different `id`. | 3 new backend tests: token minted before a password reset is rejected, token minted after is accepted, a token with no `siat` claim at all is not rejected. 113 pytest pass; `lib/actorToken.test.ts` (2 new vitest cases) confirms the claim is omitted/included correctly; 93 vitest pass; tsc clean; build clean (78 routes). |
| PK1 | 2026-07-10 "half-done features" review, item #5: the sign-in page's "Use Passkey" button was a pure UI stub — it showed a "coming soon" toast with no WebAuthn implementation, and there was no server-side passkey support (no credential storage, no registration/authentication ceremony) or client-side registration UI at all. | Backend: `webauthn` (py_webauthn) added; new `WebAuthnCredential`/`WebAuthnChallenge` DB-backed tables (challenges are one-time-use and DB-stored, not in-process — the same lesson as `IdentitySession`/C4, since Vercel serverless invocations don't share memory). New `app/services/webauthn_svc.py` + `app/api/routes/webauthn.py` expose registration (actor-authenticated: `/register/options`, `/register/verify`) and authentication (public, usernameless/discoverable-credential only — no email field, so no enumeration surface: `/authenticate/options`, `/authenticate/verify`, both rate-limited the same way as login/A5) plus `GET`/`DELETE /credentials`. RP ID is derived from `settings.frontend_url`'s hostname (defaults to `localhost` for local dev), not hardcoded. Frontend: `@simplewebauthn/browser` added; 6 thin proxy routes under `app/api/auth/webauthn/`; a second NextAuth Credentials provider (`id: "passkey"` — deliberately not `"webauthn"`, which collides with NextAuth's own adapter-requiring built-in provider type that this app, having no Adapter, can't use) hands a completed assertion to the backend for verification, mirroring the password provider. `components/auth/signInForm.tsx`'s stub `handlePasskey` replaced with the real `startAuthentication` ceremony. New `components/auth/passkeyManager.tsx` (list/add/remove passkeys via `startRegistration`) mounted at the top of `/profile`'s sidebar, ahead of "Store actions", so it's visible to every account type. | Backend: 11 new tests in `test_webauthn.py` covering challenge lifecycle (expiry, one-time-use, cross-user rejection), auth requirements, and ownership on delete — deliberately does not hand-roll a fake WebAuthn authenticator for the actual signed ceremony. That's instead proven by a new Playwright e2e test (`e2e/passkey.spec.ts`) using Chrome DevTools Protocol's real `WebAuthn` domain (a virtual authenticator with a resident/discoverable key) to register a passkey on `/profile` and sign back in with it usernameless — a real browser, real crypto, real backend verification, no mocks. 113 pytest pass; 93 vitest pass; tsc clean; eslint clean; build clean (78 routes); e2e passkey test passes. |
| DEV1 | 2026-07-11 root-cause found (originally logged 2026-07-10 as "discovered, not fixed, suspect Turbopack"). **Turbopack was not the cause** — bisected by reproducing the identical crash under `next dev --webpack` too. Real root cause: the local dev DB (`backend/data/spree_store.db`, no `DATABASE_URL` override) had accumulated 9 leftover products from repeated `pytest` runs of `_create_product_payload()` (`backend/tests/test_api.py`), each seeded with a placeholder image URL of the form `/products/test-{uuid}.jpg`. That path isn't a static file or an image route — it collides with the `/products/[id]` dynamic page route, so `next/image` requesting it gets back a full server-rendered HTML "Product unavailable" page (`200 OK`, `Content-Type: text/html`) instead of a 404. Confirmed directly: `curl http://localhost:3000/products/test-1a285bbe.jpg` → 200 HTML, ~470ms–1.6s of real SSR work per request. With every product on the home page broken this way, each page load fired 6+ of these full-page renders, which is enough load to crash the dev process under sustained/repeated hits. Real (Supabase-uploaded) product images are unaffected — they're full external URLs (`{SUPABASE_URL}/storage/v1/object/public/product-images/...`), so this only bites when a product's image field is a bare local path that happens to look like `/products/<id-shaped-thing>`. | Root cause fully identified; **not yet fixed**. The actual data cleanup (deleting the 9 `Admin Test Jacket %` rows from the local dev DB) requires explicit user confirmation — a DB mass-delete was correctly blocked by the permission classifier when attempted without it. Two independent fixes worth considering, neither applied yet: (1) local cleanup — delete the fixture-polluted rows and re-run `python -m app.seeds.catalog`; (2) app-level hardening — `/products/[id]/page.tsx` returning `200` instead of a real `404` (via Next's `notFound()`) for a nonexistent product means *any* broken/malformed image path under `/products/` will trigger this same expensive-render-instead-of-404 behavior, not just test fixtures — worth fixing regardless of the dev-data cleanup. | Confirmed via direct `curl -i` showing `Content-Type: text/html` and a "Product unavailable" `<title>` for the exact colliding path; confirmed via DB query that all 9 products currently in the local dev DB are `Admin Test Jacket %` fixture rows (zero real seeded catalog data present). Not yet covered by an automated regression test. |
| QA1 | 2026-07-11 backlog re-check: `backend/tests/test_api.py`'s only Paystack-webhook coverage (`test_webhook_retry_does_not_double_notify_order_placed`) called `orders_svc.handle_paystack_webhook()` directly, never exercising the actual `POST /api/v1/webhooks/paystack` HTTP route — so the HMAC-SHA512 signature check in `verify_webhook_signature` (`paystack.py`) had zero test coverage of the real request path, matching a QA backlog item ("needs TEST mode round-trip verification") that had gone unverified for 32+ days. While checking this, also confirmed (code-read, not a bug) that the route's `InternalAPIKey` requirement is satisfied correctly in production because Paystack's dashboard should point at the **frontend** proxy (`app/api/webhooks/paystack/route.ts`), which injects `X-Internal-Api-Key` via `proxyBackend(..., { internal: true })` — not at the backend URL directly. | Added 2 new tests going through the real HTTP route with a genuine computed HMAC-SHA512 signature (temporarily overriding `settings.payments_mock`/`paystack_secret_key`, same pattern as the existing Paystack tests in this file). | `test_paystack_webhook_route_accepts_valid_hmac_and_marks_order_paid` (valid signature → 200, order flips `pending`→`paid`) and `test_paystack_webhook_route_rejects_invalid_hmac_signature` (garbage signature → 401, order left untouched). 118 pytest pass (up from 116). Also re-checked two other 32-day-old QA backlog items: the 8 nav links that used to 404 (`/dashboard/analytics`, `/settings`, `/reports`, `/customers`, `/collections`, `/categories`, `/about`, `/help`) have all been removed from the UI entirely (zero references left in the codebase) — resolved by removal, not by adding pages. The INP perf note (Add-product ~1000ms, profile-inputs ~289ms) was re-examined but not fixed: Add-product's images already upload in parallel off the main submit path (a single `mutateAsync` call on submit, not a chain), so the latency looks like real backend processing time, not a frontend inefficiency; `profilePage.tsx` grew from ~1023 to ~1185 lines in this same session (the save/edit-mode rework), so the "large component tree, no memoization" concern is unaddressed and, if anything, slightly more true than when it was logged — left open, would need an explicit decision on component boundaries before splitting it. |
| DEV1-b | 2026-07-11 follow-up to DEV1: `app/products/[id]/page.tsx` returned HTTP 200 with a "Product unavailable" fallback UI for a genuinely nonexistent product — the underlying gap that let a broken image URL colliding with this route trigger an expensive full-page render instead of a cheap 404. | `!product` (backend cleanly returned 404, `getProductByIdOrSlug` didn't throw) now calls Next's `notFound()`, rendering the existing branded `app/not-found.tsx` boundary instead of the route's own `ProductDetailsUnavailableState`. That fallback component is kept for the *other* branch — a thrown/failed backend call (network error, 5xx) — which still shows a "try again" state rather than a hard 404, since a transient hiccup isn't "this product doesn't exist." **Caveat, confirmed while verifying:** this route has a `loading.tsx`, so Next.js streams the initial response shell with a `200` before the async page component resolves; `notFound()`'s special `NEXT_HTTP_ERROR_FALLBACK` digest and the correct not-found *content* both fire (verified in a real browser and by finding the digest in the raw SSR payload), but the outer HTTP **status code stays 200** — it's already committed by the time `notFound()` runs, a known Next.js streaming/Suspense limitation, not a bug in this fix. Making the status itself 404 would require removing this route's `loading.tsx` (trading away the loading UX) or restructuring the data fetch to resolve before any content streams — a real design tradeoff, not applied here. | `npx tsc --noEmit` clean (confirms `notFound()`'s `never` return type correctly narrows `product` afterward); `eslint` clean; 93 vitest pass. Manually verified live: `curl -i` on a colliding fake path shows the `NEXT_HTTP_ERROR_FALLBACK` digest in the streamed payload; a real Playwright browser visit renders the branded "That page doesn't exist." UI. **Not yet a complete fix for DEV1's crash** — the actual triggering data (9 pytest-fixture products with broken `/products/test-*.jpg` image URLs) is still sitting in the local dev DB; deleting those rows was proposed and requires explicit user confirmation before it's done (a DB mass-delete was correctly blocked twice by the permission classifier, including once after a general "do what you'd recommend"). |
| CH15 | 2026-07-13 live diagnostic: a console error fired once on every authenticated page load — `"You can't use a channel after client.disconnect() was called"` (stack: `getClient → getConfig → get enabled → initState → clear`, a genuine `stream-chat` SDK error string, confirmed by grepping the installed package — not Supabase, despite the diagnostic's guess). Root cause found empirically (extensive attempts to reproduce it via `chatProvider.tsx`'s customer-facing connect/user-switch effects all came back clean — that file already defers its `disconnectUser()` call via `setTimeout(0)`, precisely to avoid this race, per its own comments from the M1 fix): `components/admin/AdminChatPage.tsx`'s connect-effect cleanup called `_adminClient.disconnectUser()` **synchronously** on every unmount (e.g. an admin navigating away from `/dashboard/chat` while a conversation was open), racing the Stream `<Channel>`/`<MessageComposer>` subtree's own teardown — reproduced 100% of the time with a real admin session + Playwright. | Applied the exact same deferred-disconnect pattern `chatProvider.tsx` already uses successfully: `setTimeout(() => disconnectUser(), 0)` instead of a synchronous call, so React's passive-effect cleanups for the unmounting Stream components run first. | Reproduced the bug directly (temporary diagnostic logging + a Playwright test signing in as `admin@spree.local`, opening `/dashboard/chat`, then navigating away) — confirmed the exact reported error string fired every time before the fix, zero times after, across 3 repeated navigation cycles. Also verified chat stays functional: admin `ChannelList`/`Channel` renders across repeated visits, and the customer widget still connects and its composer is usable (typed and re-opened/closed 3×) — both with zero console errors. |
| PERF1 | 2026-07-13 live diagnostic: intermittent 503s on RSC prefetches / `HEAD /products/*` / `HEAD /cart` on first load, clearing on retry — the classic signature of Vercel Hobby-tier cold starts, not a code defect. Root cause of *why every page is so exposed to it*: `lib/serverApi.ts`'s `fetchBackend()` — the single chokepoint essentially every server-side data fetch in the app funnels through — hardcoded `cache: "no-store"` unconditionally, on every call. In Next's fetch-integrated caching model a single `no-store` fetch anywhere in a route's render path forces that *entire route* to fully dynamic, per-request rendering; a page-level `export const revalidate` cannot override an individual fetch's own cache setting. So `/`, `/products`, and `/products/[id]` all had zero caching, meaning every single page view — not just some — required a fresh Lambda invocation and a fresh backend round trip, maximizing exposure to a cold backend. | Added an opt-in `revalidate` (seconds) option to `fetchBackend`/`getJson`, defaulting to the original `no-store` behavior for every existing call site unless a caller explicitly opts in — a deliberately additive, backward-compatible change touching zero of the ~40 other callers (auth, cart, orders, admin, profile, payments all stay exactly as uncached as before). Applied `revalidate: 60` (`CATALOG_REVALIDATE_SECONDS`) to the public, non-personalized catalog reads only: `getHomeFeed`, `getProductByIdOrSlug`, `getRelatedProducts`, `getCategories`, `getBrands`, `getCollections`, and `getProducts` (via an explicit `revalidateSeconds` param passed only by `/` and `/products` — `getProducts`'s other callers, e.g. the vendor dashboard's own-catalog view and `/cart`'s recommendations, don't pass it and are unaffected). Added matching `export const revalidate = 60` (a literal — Next statically analyzes this export at build time; a reference to the constant fails the build with "Invalid segment configuration export detected") to `app/page.tsx`, `app/products/page.tsx`, `app/products/[id]/page.tsx`. | `npm run build` output confirms `○ /` (fully static, `Revalidate 1m Expire 1y`). `/products` and `/products/[id]` still show `ƒ` in the build summary — `/products` reads `searchParams` (a Request-time API that forces per-request rendering regardless of `revalidate`, verified in the docs and by inspection) so the *route* stays dynamic by design, and `/products/[id]` has no `generateStaticParams` so nothing pre-renders at build time — but the `ƒ` symbol only reflects build-time pre-rendering, not runtime cacheability: verified empirically against a real `next start` production server hitting a local backend, hammering the same URL 3× in a row — `/products/[id]` went 344ms → 49ms → 57ms, and `/products` went 131ms → 58ms, both consistent with the underlying data fetch being served from Next's cache after the first hit instead of a fresh backend call every time. Per the task's own instruction, no keep-warm cron was added; if sub-second cold-start latency matters, upgrading off the Hobby tier remains the actual fix — this change only reduces how often a cold start is hit, not its duration. |
| SEC1 | 2026-07-13 session/auth hardening request, Fix 1: every route returned no enforcing `Content-Security-Policy` and no `X-Frame-Options` at all — `next.config.ts` already had a CSP, but only as `Content-Security-Policy-Report-Only` (logs violations, blocks nothing), and its `img-src`/`connect-src` predated Stream Chat and Paystack Inline being added to the app (both confirmed via code + by fetching js.paystack.co's actual source: it uses `.openIframe()` against `checkout.paystack.com`, calls `api.paystack.co`, and loads a stylesheet from `paystack.com`; Stream Chat's SDK default `baseURL`/`wsBaseURL` is `chat.stream-io-api.com`). Also confirmed there is **no Supabase Realtime usage anywhere** in the codebase (no `@supabase/supabase-js` dependency at all) — Supabase is server-side-only (Postgres + a plain `fetch()` to its Storage REST API for uploads); the app's actual realtime feature is Stream Chat. | Added `X-Frame-Options: DENY` as a real, immediately-enforcing header (closes the clickjacking gap today, independent of CSP). Kept CSP in Report-Only — Next's own RSC hydration payload ships via inline `<script>` tags with no nonce infrastructure set up, so enforcing `script-src 'self'` as-is would break hydration in production; flipping to enforcing is a separate, larger follow-up. Rewrote the CSP directive list with an evidence-based allowlist: `img-src` narrowed from `https:` (any host) to `'self' data: blob: https://*.supabase.co https://placehold.co` (the two hosts `components/ui/productImage.tsx`'s unoptimized-retry fallback can actually hit, per `backend/app/seeds/catalog.py`'s placeholder URLs and vendor-uploaded Supabase images — `blob:` kept for `productCreateForm.tsx`'s upload previews); `connect-src` gained `https://chat.stream-io-api.com wss://chat.stream-io-api.com` (Stream Chat, the real realtime mechanism — not Supabase) and `https://api.paystack.co`; `script-src` gained `https://js.paystack.co` and (found via a live CSP-violation check, see Test column) `https://va.vercel-scripts.com` (`@vercel/analytics`'s dev-mode script); new `frame-src https://checkout.paystack.com https://paystack.com` (previously unset, would have fallen back to `default-src 'self'` and blocked Paystack's iframe checkout entirely once enforced); new `style-src` entry for `https://paystack.com` (its inline script injects its own button CSS from there). Also added a path-scoped `Vary: Cookie` on `/api/auth/:path*` (Fix 2's one actionable item — cookie `httpOnly`/`secure`/`sameSite` are already Auth.js's untouched defaults, no custom `cookies` config exists in `auth.ts`/`auth.config.ts`, and `NEXTAUTH_URL` is already configured on Vercel for Production, confirmed via `vercel env ls`). **Fix 3 (localStorage PII) does not apply**: exhaustively searched the repo for `spree-seller-wizard-draft`/`spree-vendor-wizard-draft` and any `localStorage`/`persist(` usage under `components/vendor/` — neither exists. The vendor onboarding wizard (`SellerOnboardingWizard.tsx`) persists its draft entirely server-side (`api.getOnboardingState()`/`api.saveOnboardingStep()`); the only 4 localStorage-backed stores in the app are `spree-cart`, `spree-favorites`, `spree-catalog-filters`, `spree-settings`, none of which hold name/email/address. No code change made for Fix 3 — reported as not applicable rather than inventing a fix for a store that doesn't exist. | `npm run build`/`tsc`/`eslint` clean; 93 vitest pass. Live-verified emitted headers via `curl -i`: `X-Frame-Options: DENY` and the full updated CSP present on `/`; `Vary: Cookie` (alongside Next's own pre-existing router-state `Vary`) plus the pre-existing `Cache-Control: private, no-cache, no-store` present on `/api/auth/session`. Verified with a live Playwright session (`document.addEventListener("securitypolicyviolation", …)`, which fires even in Report-Only mode) across signup → profile → opening/closing the support chat widget: zero violations after adding `va.vercel-scripts.com` (one real violation *was* caught and fixed this way, proving the check works). Did not drive a full live checkout — `.env.local` has a **live** (not test) Paystack public key, and completing checkout requires filling the whole shipping form — so the Paystack allowlist entries are verified by direct inspection of `js.paystack.co`'s actual source rather than a live run; Report-Only mode means an unverified miss there would surface as a console warning, not breakage. |
| PK2 | 2026-07-11: PK1's passkey backend was fully implemented and merged (eb5a9e1), but every `/api/v1/auth/webauthn/*` route 404'd in production (`spree-backend.vercel.app`) — OAuth and every other `/auth/*` route worked fine, isolating it to just this router. Root cause: the requirements.txt pin from G13 (`cryptography>=43.0.0,<45.0.0`) and PK1's own new pin (`webauthn>=3.0.0,<4.0.0`, which requires `cryptography>=49.0.0`) were mutually unsatisfiable. `pip install` (local dev, and this repo's own CI `backend` test job) silently tolerated the conflict and installed `cryptography==49.0.0` anyway — but Vercel's build uses `uv lock`, which does a strict SAT-style resolve and correctly refused to build at all, confirmed via `vercel inspect --logs` on the errored deployment. The whole deploy failed, so Vercel kept serving the *previous* (pre-PK1) production build — hence instant, contentless 404s indistinguishable from "route doesn't exist." | Raised the `cryptography` pin to `>=49.0.0,<50.0.0` (only `Fernet`/`InvalidToken` are used by `encryption.py`'s G13 field encryption — a stable API across that range). Also, per this task's explicit ask: split the RP ID/origin config into two independently-settable fields, `WEBAUTHN_RP_ID` and `WEBAUTHN_RP_ORIGIN` (previously only RP ID had an override, and it was derived-only with no origin override at all) — both still fall back to `frontend_url` when unset, so no new Vercel env var is *required* (confirmed `FRONTEND_URL` is already configured on `spree-backend`); documented concrete production values (`spreecommerce.vercel.app` / `https://spreecommerce.vercel.app`, confirmed via `vercel inspect` on the frontend project's aliases) in `backend/.env.example`. Added structured `safe_extra()` logging (register/authenticate success and failure, keyed by `webauthn_event`) — the actual error paths were already structured JSON via `HTTPException`, so the "bare 404/500" symptom was entirely the failed-deploy issue above, not an error-handling gap. | Verified the actual failure mode directly against production (`vercel ls`/`vercel inspect --logs` on the errored deployment) before touching anything, rather than assuming the code was wrong. `npx vercel build --yes` (the same `uv lock`-based build Vercel's CI runs) now succeeds locally against the new pin. Added 3 new real-crypto tests to `test_webauthn.py` via a new `FakeAuthenticator` helper (real P-256 keypair, real ECDSA signatures, CBOR-encoded COSE key, "none" attestation) — previously the suite only exercised error paths with garbage credential payloads: `test_register_verify_persists_a_real_credential` (asserts the DB row's `credential_id`/`public_key`/`sign_count`), `test_authenticate_verify_succeeds_and_increments_sign_count` (asserts the returned user shape and that `sign_count` moves 0→1), `test_authenticate_verify_rejects_a_replayed_challenge_even_with_a_valid_signature` (same signed assertion resubmitted is rejected — proves replay protection isn't just "garbage signatures fail"). 116 pytest pass (up from 113). |
